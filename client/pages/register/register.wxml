<view class="userinfo">
        <image
            class="userinfo-avatar"
            src="{{logged ? userInfo.avatarUrl : './user-unlogin.png'}}"
            background-size="cover"
        />
        <view>
            <text class="userinfo-nickname">{{logged ? userInfo.nickName : '未填入'}}</text>
        </view>
    </view>
